{
  "name": "fund-gallery",
  "version": "1.0.0",
  "description": "The FUND Gallery - Supporting young artists through community raffles",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build && cd .. && ./scripts/post-build.sh",
    "start": "cd frontend && npm start",
    "dev": "cd frontend && npm run dev",
    "migrate": "cd frontend && npm run migrate",
    "seed": "cd frontend && npm run seed"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}